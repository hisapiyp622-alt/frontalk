# img/ — アイコンと画像

| ファイル | 用途 |
|---|---|
| `icon-192.png` `icon-512.png` | ホーム画面に置いたときのアイコン |
| `icon-maskable-512.png` | Androidの丸/角丸切り抜き用 |
| `apple-touch-icon.png` | iPad・iPhone のホーム画面用 |

アイコンは**赤地に白抜きの「見積」**です。`../icon.svg` と同じ考え方で、
`tools/` の外から作り直す場合も同じ意匠にそろえてください。

## マスコットについて（2026-08-18 に外しました）

以前はマスコット「ミツモリン」のイラストをアイコンと使い方案内に使っていました。
**フロントークへの改名にあわせて、キャラクターはいったん外しています。**

- `mitsumorin-hello.png` … 使い方案内・ログイン画面で使っていた立ち絵
- `mitsumorin-sheet.png` … 見積書の説明で使っていた立ち絵

**この2枚は消さずに残してあります。**戻すときは、
`app.js` の使い方案内（`TOUR_STEPS` のあたり）に画像を出す処理を足し、
`index.html` のログイン画面・担当者コード画面に `<img>` を戻してください。
Service Worker の事前キャッシュ（`sw.js` の `ASSETS`）への追加もお忘れなく。
